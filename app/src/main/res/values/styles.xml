<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ===== THÈME PRINCIPAL ===== -->
    <style name="Theme.BeautyManager" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/bg_blanc</item>
    </style>

    <!-- ===== THÈME DIALOG ===== -->
    <style name="Theme.BeautyManager.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!-- Style pour SearchView -->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/transparent</item>
        <item name="searchHintIcon">@null</item>
    </style>

    <!-- Style pour MaterialButton -->
    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <item name="android:minHeight">48dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>
    <!-- ===== APPEARANCE TEXTE ===== -->
    <style name="TextAppearance.BeautyManager.Subtitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/violet_fonce</item>
    </style>

    <style name="TextAppearance.BeautyManager.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/texte_gris</item>
    </style>
    <style name="TextAppearance.BeautyManager">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/texte_noir</item>
    </style>
    <!-- AJOUTEZ CE STYLE EN PREMIER dans styles.xml -->
    <style name="TextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/texte_noir</item>
    </style>
    <!-- ===== CARDVIEW ===== -->
    <style name="CardView.BeautyManager" parent="CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/bg_blanc</item>
    </style>

    <!-- ===== TEXT INPUT LAYOUT ===== -->
    <style name="Widget.MaterialComponents.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/violet_moyen</item>
        <item name="hintTextColor">@color/violet_moyen</item>
        <item name="android:textColorHint">@color/violet_moyen</item>
    </style>
</resources>